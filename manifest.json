{
	"manifest_version": 2,
	"name": "Granblue Raid Helper",
	"description": "This extension will insert the first item in the clipboard when on Granblue input raid screen.",
	"version": "1.0",
	"content_scripts": [{
        "js": ["contentscript.js"],
        "matches": ["http://localhost:8080/*"]
    }],
	"permissions": [
		"clipboardRead",
		"activeTab",
		"http://localhost:8080/*"
	]
}